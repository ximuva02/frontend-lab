//gnerate a story for the ChatPanel component in chatPanel.tsx
import ChatPanel from "./chatPanel";

export default {
  title: "Components/Chat/ChatPanel",
  component: ChatPanel,
};

export const Default = () => (
  <div style={{ height: "40vh" }}>
    <ChatPanel />
  </div>
);
